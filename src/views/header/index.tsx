import * as DOM from 'DOM'
import cn from 'classnames'
import style from './style.scss'
// import logo from '../logo'



export default (
  <header class={cn(style.header)}>
    {/* {logo.cloneNode(true)} */}
    <h1>
      BSunderhus
    </h1>
  </header>
)
